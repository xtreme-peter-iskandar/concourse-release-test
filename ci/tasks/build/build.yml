platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: jdk

inputs:
  - name: concourse-release

run:
  path: ./concourse-releasegit add/ci/tasks/build/build.sh